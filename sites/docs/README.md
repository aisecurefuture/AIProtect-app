# aiprotect/docs — `docs.aiprotect.app`

**Status:** not built. Built by Prompt 9.

Fork of the repo-root `docs-site/` (MkDocs).

## Audience
Consumers, not administrators. Setup per device, what each feature actually
protects against, and a plain-language privacy FAQ.

`support.aiprotect.app` is a separate property — evaluate a static help center
plus a hosted helpdesk before building anything custom.
